<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.romariogrohmann.sistemagestaodevisitas">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".incluirvisitas" />
        <activity android:name=".finalizavisita" />
        <activity android:name=".pendencias" />
        <activity android:name=".relatorios" />
        <activity android:name=".agenda" />
        <activity android:name=".dadosVisita" />
        <activity android:name=".telainicial" />
        <activity android:name=".visitasdia" />
        <activity
            android:name=".login"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>